# 项目逻辑文档：狼人杀/桌游积分记录器

本项目是一个微信小程序，专为社交游戏（如狼人杀、各种桌游）的积分记录和结算而设计。它使用**微信云开发（Tencent CloudBase）**作为后端，提供实时数据同步和持久化存储功能。

## 1. 系统架构

- **前端**：微信小程序原生开发 (WXML / WXSS / JS)。
- **后端**：云开发 CloudBase（云函数 & NoSQL 云数据库）。
- **实时同步**：利用云数据库的 `watch` 监听能力，确保所有玩家能瞬间看到相同的分数变化。

## 2. 核心逻辑

### A. 数据模型 (Data Models)
- **Rooms (房间)**：存储游戏对局的元数据（房主、6位房间号、当前台面费、状态等）。
- **Players (玩家)**：记录房间内的参与者信息。
  - *真实玩家*：通过微信 `openid` 绑定。
  - *虚拟玩家*：由房主手动添加，用于线下玩家的积分记录。
- **Rounds (对局流水)**：每一次记账操作的不可变审计记录（流水账）。

### B. 零和原则 (The Zero-Sum Principle)
系统的数据完整性依赖于**零和规则**：
> `Sum(所有玩家分数变化) + 台面费变化 = 0`

该规则在 `submitRound` 云函数中被严格校验，以防止数据不一致或“分数凭空消失/增加”的情况。

### C. 记账工作流 (Scoring Workflows)
1. **快捷记账**：直接点击某个玩家，进行一对一的交易记录（例如：“支出给玩家Y X分”）。
2. **批量记账**：
   - **支出（Expense）**：用户同时向多名玩家支付分数。
   - **收入（Income）**：用户同时从多名玩家处收取分数。
   - **倍数/底分支持**：用户可以设置一个“单份金额（unitAmount）”，并通过倍数按钮（1倍、2倍等）快速批量输入分数。
3. **台面费 (Table Fee)**：分数可以从玩家转移到“房间”（台面费），用于支付场地费、游戏成本等。

### D. 房间生命周期 (Room Lifecycle)
1. **创建**：房主创建房间并获得一个6位数的房间码。
2. **加入**：玩家可以通过输入房间码、点击分享链接或扫描生成的二维码加入。
3. **管理**：房主可以踢出玩家（状态变为 `is_kicked`，保留余额用于对账）或恢复被踢出的玩家。
4. **实时监控**：前端 `playerWatcher` 监听 `Players` 集合的变化并瞬间更新 UI。采用“分数指纹（score fingerprint）”机制，智能判断何时需要拉取最新的流水记录，极大优化了性能。

### E. 用户统计 (User Statistics)
`getHomeData` 云函数聚合了用户在所有历史 `Players` 记录中的数据：
- 总胜场/负场（基于 `current_score > 0` 判断）。
- 胜率与平均分。
- 历史参与的房间列表（排除用户自行隐藏的房间）。

## 3. 关键技术实现

- **云函数 `room-manager`**：集中处理所有核心业务逻辑（创建、加入、提交对局记账等），避免前端直接操作数据库导致的安全问题。
- **并发优化**：前端采用极致的单监听器策略，通过对比分数特征值按需局部刷新，大幅降低了 WebSocket 连接数开销，即便在云开发基础版配额下也能稳定支持 10-18 人同时在线。
- **数据一致性**：在核心的记账写入逻辑中，使用数据库的 `_.inc()` 原子操作来更新分数，有效防止了高频多人记账时的并发竞态条件（Race Conditions）。

## 4. Web 管理后台 (Admin Web)

项目包含一个静态网页版的管理后台 (`admin-web/`)，供房主（管理员）在 PC 端管理自己创建的房间和玩家数据。
- **登录机制**：支持**扫码登录**（通过临时 sessionToken）和**账号密码直连**（验证 uid 是否具备房主权限）双通道。
- **核心操作**：在后台可修正玩家异常分数、强制结算/删除异常房间，以及一键批量清理超时活跃房间。
- **权限安全**：通过配置 CloudBase 的 Web 安全域名（允许跨域）并开启匿名登录以支持接口联通；所有敏感写操作由 `adminAction` 云函数严格拦截并进行二次身份鉴权。

## 5. AI 大模型集成 (混元体验通道)

本项目在结算页面 (`pages/room/result`) 创新性地引入了 AI “毒舌战报”功能，用于增强打牌社交分享的情绪价值。
- **调用方式**：前端直接调用微信官方基础库内置的 `wx.cloud.extend.AI`。
- **优势**：该通道属于微信生态默认免费体验池，不仅无需在小程序端安装额外的 SDK，而且**完全不消耗开发者个人的 CloudBase Token 额度**，实现了轻量级“白嫖”。
- **架构解耦**：AI 战报生成仅作为前端锦上添花的展示，生成失败或超时均不会阻塞或影响核心的扣分与账单流水逻辑。

---
*由 Antigravity 生成*
